<%= render 'layouts/breadcrumbs' %>

<div class="page-heading">
  <h2>Edit Experiment</h2>
  <div class="buttons">

    <%= link_to experiment_path(@experiment), method: :delete, data: { modal_confirm: 'Are you sure you want to delete this experiment and all its data?' }, class: 'ui button' do %>
      <i class="remove icon"></i> Delete Experiment
    <% end %>
  </div>
</div>

<%= render 'form', experiment: @experiment %>
